import classes  from './PredictionSelect.module.css';

const PredictionSelect = (props) => {
  return (
    <div className={classes['bboxSelector']}>
      <p>test</p>
    </div>
  );
};

export default PredictionSelect;